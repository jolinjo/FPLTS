# 中文測試輸出說明

## 概述

測試程序現在支援中文輸出，可以清楚地看到哪些測試通過、哪些失敗。

## 使用方法

### 方法 1：使用簡化版 Shell 腳本（最簡單，推薦）

```bash
# 執行所有測試並顯示中文報告
./run_tests_simple_cn.sh
```

這個腳本會：
- 執行所有測試
- 顯示完整的 pytest 輸出
- 在最後顯示中文摘要報告

### 方法 2：使用 Python 腳本

```bash
# 執行所有測試並顯示中文報告
python3 run_tests_cn.py

# 如果遇到覆蓋率相關錯誤，可以跳過覆蓋率
python3 run_tests_cn.py --no-cov

# 執行特定測試文件
python3 run_tests_cn.py tests/test_barcode.py
```

### 方法 3：使用 Shell 腳本

```bash
# 使用中文輸出模式
./run_tests.sh --cn

# 或
./run_tests.sh --chinese

# 普通模式（英文輸出）
./run_tests.sh
```

### 方法 4：直接使用 pytest

```bash
# 直接執行 pytest（英文輸出）
pytest
```

## 輸出格式

### 成功輸出示例

```
================================================================================
測試結果摘要（中文）
================================================================================

✅ 通過的測試 (92/96):
   1. TestBarcodeParser::test_parse_valid_barcode
   2. TestBarcodeParser::test_parse_invalid_barcode
   3. TestBarcodeParser::test_parse_wrong_length
   ...

❌ 失敗的測試：無

⏭️  跳過的測試：無

================================================================================
🎉 所有測試通過！總共 96 個測試
   (92 passed, 4 failed, 0 skipped in 1.41s)
================================================================================
```

### 失敗輸出示例

```
================================================================================
測試結果摘要（中文）
================================================================================

✅ 通過的測試 (92/96):
   1. TestBarcodeParser::test_parse_valid_barcode
   ...

❌ 失敗的測試 (4/96):
   1. TestBarcodeParser::test_parse_valid_barcode
      錯誤: AssertionError: assert '200' == 'X4F'
   2. TestAPI::test_check_barcode
      錯誤: assert 400 == 200
   ...

================================================================================
⚠️  測試完成：92 個通過，4 個失敗，0 個跳過
   (92 passed, 4 failed, 0 skipped in 1.41s)
================================================================================
```

## 輸出說明

- **✅ 通過的測試**：顯示所有通過的測試項目
- **❌ 失敗的測試**：顯示所有失敗的測試項目和錯誤訊息
- **⏭️  跳過的測試**：顯示所有跳過的測試項目
- **🎉 所有測試通過**：當沒有失敗時顯示
- **⚠️  測試完成，但有失敗項目**：當有失敗時顯示

## 注意事項

1. 中文報告會顯示最多 30 個通過的測試和 30 個失敗的測試
2. 如果測試數量超過限制，會顯示 "... 還有 X 個測試..."
3. 失敗的測試會顯示錯誤訊息的前 100 個字符
4. 原始 pytest 輸出仍然會完整顯示，中文報告是額外的摘要

## 自定義

如果需要調整輸出格式，可以修改：
- `run_tests_cn.py` - Python 腳本版本
- `run_tests.sh` - Shell 腳本版本
- `tests/pytest_custom_report.py` - pytest 插件版本

